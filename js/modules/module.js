neo.Module = {
  tag : {},
  
  /**
   * @param Object with options or false. 
   * 
   * Options are loaded from the "data-options" attribute set in the tag element. It accepts any valid javascript expression.
   */
  construct : function(options) {
    // overridden by modules
  }
}